"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import {useEffect,useState} from "react";import {AdminShell,ErrorState,LoadingState,money} from "@/app/_components/admin-shell";
export default function ReportsPage(){const[data,setData]=useState<any>();const[state,setState]=useState("loading");useEffect(()=>{fetch("/api/dashboard").then(async r=>({ok:r.ok,data:await r.json()})).then(({ok,data})=>{setData(ok?data:null);setState(ok?"ready":"error")}).catch(()=>setState("error"))},[]);return <AdminShell title="تقارير المستثمر" subtitle="ملخص مالي للقراءة فقط، دون بيانات المتدربين الخاصة." active="/reports">{state==="loading"?<LoadingState/>:state==="error"?<ErrorState message="هذه التقارير متاحة للمستثمر والإدارة فقط."/>:<section className="data-grid"><Card label="إيرادات الشهر" value={money(data.kpis.monthlyIncome)}/><Card label="مصروفات الشهر" value={money(data.kpis.monthlyExpenses)}/><Card label="صافي الربح" value={money(data.kpis.netProfit)}/><Card label="مستحقات الكباتن" value={money(data.kpis.outstandingCoachPayments)}/></section>}</AdminShell>};function Card({label,value}:{label:string;value:string}){return <article className="panel"><h3>{label}</h3><strong>{value}</strong></article>}
