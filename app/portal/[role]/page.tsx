"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link"; import { FormEvent, useEffect, useState } from "react"; import { useParams, useRouter } from "next/navigation"; import { money } from "@/app/_components/admin-shell";
type Profile={role:"admin"|"coach"|"investor"|"customer";full_name:string;email:string;staffId?:string|null;memberId?:string|null};
const links={admin:[["إدارة المتدربين","/members"],["إدارة الكباتن","/coaches"],["الدورات والجداول","/courses"],["الحسابات","/finance"],["المستخدمون","/users"]],coach:[["دوراتي","/courses"],["ملفي ومستحقاتي","/coaches"],["المتدربون المخصصون","/members"]],investor:[["التقرير المالي","/reports"],["الحركات المالية","/finance"]],customer:[["ملفي الرياضي","/members"],["المتجر","/shop"]]} as const;
export default function Portal(){const params=useParams<{role:Profile["role"]}>();const router=useRouter();const[profile,setProfile]=useState<Profile|null>(null);const[summary,setSummary]=useState<any>(null);const[message,setMessage]=useState("");const[showForm,setShowForm]=useState(false);useEffect(()=>{fetch("/api/auth/me").then(async r=>({ok:r.ok,data:await r.json()})).then(({ok,data})=>{if(!ok||data.role!==params.role){router.replace("/login");return;}setProfile(data);if(data.role==="admin"||data.role==="investor")fetch("/api/dashboard").then(r=>r.ok?r.json():null).then(setSummary);}).catch(()=>router.replace("/login"));},[params.role,router]);async function createUser(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({fullName:f.get("name"),email:f.get("email"),password:f.get("password"),role:f.get("role")})});const d=await r.json();setMessage(r.ok?"تم إنشاء المستخدم وتفعيل حسابه.":d.error);if(r.ok)setShowForm(false)}async function logout(){await fetch("/api/auth/logout",{method:"POST"});router.push("/login")}if(!profile)return <main className="portal-loading" dir="rtl">جارِ فتح بوابتك…</main>;const target=profile.role==="coach"&&profile.staffId?`/coaches/${profile.staffId}`:profile.role==="customer"&&profile.memberId?`/members/${profile.memberId}`:null;const cards=summary?[['دخل الشهر',money(summary.kpis.monthlyIncome)],['مصروفات الشهر',money(summary.kpis.monthlyExpenses)],['صافي الربح',money(summary.kpis.netProfit)]]:profile.role==="coach"?[["ملفك المالي","عرض المستحقات"],["دوراتك","عرض الجدول"]]:profile.role==="customer"?[["ملفك الرياضي","اشتراكات وحضور"],["المتجر","تصفح المنتجات"]]:[["التقارير","وصول للقراءة فقط"]];return <main dir="rtl" className="portal-page"><header className="portal-top"><Link className="brand" href="/"><span className="brand-mark">ن</span>ناديك<span className="brand-dot">.</span></Link><div><span>{profile.full_name}</span><button onClick={logout}>تسجيل الخروج</button></div></header><section className="portal-intro"><div><p className="eyebrow">بوابتك الخاصة</p><h1>{profile.role==="admin"?"مركز إدارة النادي":profile.role==="coach"?"حساب الكابتن":profile.role==="investor"?"بوابة المستثمر":"بوابة المتدرب"}</h1><p>تظهر لك البيانات المسموح بها فقط وفق دور حسابك.</p></div>{profile.role==="admin"&&<button className="primary-button" onClick={()=>setShowForm(true)}>＋ إنشاء مستخدم</button>}</section>{message&&<p className="portal-message">{message}</p>}<section className="portal-stats">{cards.map(([label,value])=><article key={label}><small>{label}</small><b>{value}</b><span>بيانات حية من النظام</span></article>)}</section><section className="portal-grid"><article className="portal-panel"><h2>اختصاراتك</h2><div className="portal-actions">{target&&<Link href={target}>ملفي التفصيلي <span>←</span></Link>}{links[profile.role].map(([label,href])=><Link key={label} href={href}>{label}<span>←</span></Link>)}</div></article><article className="portal-panel"><h2>الحماية والصلاحيات</h2><div className="audit-list"><span><b>وصول مقيد</b><small>تُفحص الصلاحية في الخادم قبل تحميل البيانات.</small></span><span><b>سجل مالي موثق</b><small>العمليات المالية لا تعدّل من الواجهة.</small></span></div></article></section>{showForm&&<div className="modal-backdrop"><section className="modal"><button className="modal-close" onClick={()=>setShowForm(false)}>×</button><form onSubmit={createUser}><h2>إنشاء مستخدم</h2><label>الاسم<input name="name" required/></label><label>البريد<input name="email" type="email" required/></label><label>كلمة المرور<input name="password" type="password" minLength={10} required/></label><label>الدور<select name="role"><option value="coach">كوتش</option><option value="investor">مستثمر</option><option value="customer">متدرب</option><option value="admin">إدارة</option></select></label><button className="primary-button wide">إنشاء الحساب</button></form></section></div>}</main>}
