"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import {useEffect,useState} from "react";import {AdminShell,EmptyState,ErrorState,LoadingState,money,StatusBadge} from "@/app/_components/admin-shell";
export default function StorePage(){const[data,setData]=useState<any[]>([]);const[state,setState]=useState("loading");useEffect(()=>{fetch("/api/records/store").then(async r=>({ok:r.ok,data:await r.json()})).then(({ok,data})=>{setData(ok?data.records:[]);setState(ok?"ready":"error")}).catch(()=>setState("error"))},[]);return <AdminShell title="المتجر والمخزون" subtitle="منتجات ومخزون حقيقي من قاعدة البيانات." active="/store">{state==="loading"?<LoadingState/>:state==="error"?<ErrorState message="تعذر تحميل مخزون المتجر."/>:data.length===0?<EmptyState title="لا يوجد منتجات" detail="أضف منتجات من إدارة المخزون."/>:<article className="panel table-card"><div className="data-table"><table><thead><tr><th>المنتج</th><th>التفاصيل</th><th>السعر</th><th>الحالة</th></tr></thead><tbody>{data.map(p=><tr key={p.id}><td><b>{p.name}</b></td><td>{p.detail}</td><td className="course-price">{money(p.amount)}</td><td><StatusBadge>{p.status}</StatusBadge></td></tr>)}</tbody></table></div></article>}</AdminShell>}
