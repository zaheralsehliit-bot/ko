"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link"; import {useEffect,useState} from "react"; import {useParams} from "next/navigation"; import {AdminShell,EmptyState,ErrorState,LoadingState,money,StatusBadge} from "@/app/_components/admin-shell";
export default function CourseProfile(){const{id}=useParams<{id:string}>();const[data,setData]=useState<any>();const[state,setState]=useState("loading");useEffect(()=>{fetch(`/api/courses/${id}`).then(async r=>({ok:r.ok,data:await r.json()})).then(({ok,data})=>{setData(ok?data:null);setState(ok?"ready":"error")}).catch(()=>setState("error"))},[id]);if(state==="loading")return <AdminShell title="تفاصيل الدورة" active="/courses"><LoadingState/></AdminShell>;if(!data)return <AdminShell title="تفاصيل الدورة" active="/courses"><ErrorState message="تعذر تحميل الدورة أو لا تملك صلاحية الوصول."/></AdminShell>;const c=data.course;const active=data.subscriptions.filter((s:any)=>s.status==="نشط");return <AdminShell title="تفاصيل الدورة" subtitle="سعر وجداول ومتدربون وإيراد الدورة من البيانات الفعلية." active="/courses"><div className="profile-hero"><span className="avatar violet">د</span><div><h2>{c.name}</h2><p>{c.course_code||c.category||"دورة"} · {c.level||"جميع المستويات"} · <span className="course-price">{money(c.monthly_price)}</span></p></div><div className="profile-actions">{c.staff&&<Link className="secondary-button" href={`/coaches/${c.staff.id}`}>ملف الكابتن</Link>}<Link className="secondary-button" href="/courses">رجوع للقائمة</Link></div></div><section className="data-grid"><article className="panel"><h3>السعر الشهري</h3><strong className="course-price">{money(c.monthly_price)}</strong><p>{c.duration_days?`${c.duration_days} يوم`:(c.sessions_per_week?`${c.sessions_per_week} حصص أسبوعياً`:c.schedule)}</p></article><article className="panel"><h3>الطاقة الاستيعابية</h3><strong>{active.length} / {c.capacity}</strong><p>{Math.max(0,c.capacity-active.length)} مقاعد متاحة</p></article><article className="panel"><h3>إيراد الاشتراكات النشطة</h3><strong>{money(data.revenue)}</strong><p><StatusBadge>{c.status}</StatusBadge></p></article></section><section className="split-grid"><article className="panel"><h2>الوصف والبرنامج</h2><p className="account-note">{c.full_description||c.short_description||"لم يضف وصف تفصيلي بعد."}</p><div className="data-list">{data.sessions.length?data.sessions.map((s:any)=><div key={s.id}><span><b>اليوم {s.day_of_week}</b><small>{s.room||"القاعة الرئيسية"}</small></span><span>{s.start_time} – {s.end_time}</span></div>):<EmptyState title="لا توجد حصص مسجلة" detail="أضف جلسات الدورة لتظهر هنا."/>}</div></article><article className="panel"><h2>الكابتن</h2>{c.staff?<div className="data-list"><div><span><b>{c.staff.full_name}</b><small>{c.staff.specialties||"كابتن الدورة"}</small></span><Link className="detail-link" href={`/coaches/${c.staff.id}`}>الملف ←</Link></div></div>:<EmptyState title="لا يوجد كابتن" detail="عيّن كابتناً للدورة."/>}</article></section><section className="split-grid"><article className="panel"><h2>المتدربون المسجلون</h2>{active.length?<div className="data-list">{active.map((s:any)=><div key={s.id}><span><b>{s.members?.full_name||"متدرب"}</b><small>{s.members?.member_code} · ينتهي {s.end_date}</small></span><Link className="detail-link" href={`/members/${s.members?.id}`}>الملف ←</Link></div>)}</div>:<EmptyState title="لا يوجد تسجيلات" detail="لم يسجل متدربون في هذه الدورة."/>}</article><article className="panel"><h2>ملخص الحضور</h2><strong>{data.attendance.filter((a:any)=>a.status==="حاضر").length} حضور</strong><p>{data.attendance.length} حركة حضور مسجلة للدورة.</p></article></section></AdminShell>}
