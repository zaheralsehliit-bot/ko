"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {AdminShell,ErrorState,LoadingState,money} from "@/app/_components/admin-shell";
type Dashboard={kpis:{activeMembers:number;expiringMembers:number;monthlyIncome:number;monthlyExpenses:number;netProfit:number;outstandingCoachPayments:number;lowStock:number};movements:Array<{id:string;title:string;account_name:string;category:string;amount:number;direction:"in"|"out";payment_method:string;occurred_at:string}>};
export default function Home(){const[data,setData]=useState<Dashboard|null>(null);const[state,setState]=useState("loading");useEffect(()=>{fetch("/api/dashboard").then(async r=>({ok:r.ok,data:await r.json()})).then(({ok,data})=>{setData(ok?data:null);setState(ok?"ready":"error")}).catch(()=>setState("error"))},[]);if(state==="loading")return <AdminShell title="لوحة الإدارة" active="/"><LoadingState/></AdminShell>;if(!data)return <AdminShell title="لوحة الإدارة" active="/"><ErrorState message="تعذر تحميل مؤشرات الإدارة. سجّل الدخول كمدير وتحقق من قاعدة البيانات."/></AdminShell>;const k=data.kpis;return <AdminShell title="صباح الخير، زاهر" subtitle="ملخص حيّ لحركة النادي لهذا الشهر." active="/" action={<div className="header-actions"><Link className="secondary-button" href="/members">إدارة المتدربين</Link><Link className="primary-button" href="/finance">الحسابات والفواتير</Link></div>}><section className="metrics-grid"><Metric label="المتدربون النشطون" value={String(k.activeMembers)} detail="من قاعدة البيانات"/><Metric label="تجديدات خلال 7 أيام" value={String(k.expiringMembers)} detail="تحتاج متابعة"/><Metric label="دخل الشهر" value={money(k.monthlyIncome)} detail="حركات قبض موثقة"/><Metric label="مصروفات الشهر" value={money(k.monthlyExpenses)} detail="حركات سحب موثقة"/><Metric label="صافي الربح" value={money(k.netProfit)} detail="الدخل ناقص المصروفات"/><Metric label="مستحقات الكباتن" value={money(k.outstandingCoachPayments)} detail="غير مدفوعة"/><Metric label="مخزون منخفض" value={String(k.lowStock)} detail="منتجات أقل من 10 قطع"/></section><section className="dashboard-grid"><article className="panel"><div className="panel-heading"><div><h2>آخر الحركات المالية</h2><p>كل حركة مسجلة من دفتر الحسابات.</p></div><Link href="/finance">عرض الكل ←</Link></div>{data.movements.length?<div className="data-list">{data.movements.map(m=><div key={m.id}><span><b>{m.title}</b><small>{m.account_name} · {m.category} · {new Date(m.occurred_at).toLocaleDateString("ar-SY")}</small></span><strong className={m.direction==="in"?"green-text":"orange-text"}>{m.direction==="in"?"+":"−"}{money(m.amount)}</strong></div>)}</div>:<p className="empty-state">لا توجد حركات مالية مسجلة.</p>}</article><article className="panel"><div className="panel-heading"><div><h2>وصول سريع</h2><p>انتقل إلى الصفحة الكاملة لكل قسم.</p></div></div><div className="quick-actions"><Link className="secondary-button" href="/members">المتدربون</Link><Link className="secondary-button" href="/coaches">الكباتن</Link><Link className="secondary-button" href="/courses">الدورات</Link><Link className="secondary-button" href="/store">المخزون</Link><Link className="secondary-button" href="/reports">التقارير</Link><Link className="secondary-button" href="/users">المستخدمون</Link></div></article></section></AdminShell>}
function Metric({label,value,detail}:{label:string;value:string;detail:string}){return <article className="metric-card"><div className="metric-icon purple">◈</div><div className="metric-label"><span>{label}</span><small className="positive">حيّ</small></div><strong>{value}</strong><p>{detail}</p></article>}
