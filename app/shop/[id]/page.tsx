"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import Image from "next/image";
import Link from "next/link";
import {useEffect,useState} from "react";
export default function ProductPage({params}:{params:Promise<{id:string}>}){const[product,setProduct]=useState<any>(null);const[error,setError]=useState("");useEffect(()=>{params.then(({id})=>fetch(`/api/public/products/${id}`).then(async r=>({ok:r.ok,data:await r.json()})).then(r=>r.ok?setProduct(r.data.product):setError(r.data.error||"المنتج غير متاح.")))},[params]);if(error)return <main className="ko-site"><section className="ko-section"><Link href="/shop">← المتجر</Link><p className="ko-alert">{error}</p></section></main>;if(!product)return <main className="ko-site"><section className="ko-section"><div className="ko-loading"/></section></main>;return <main className="ko-site"><header className="ko-nav"><Link href="/" className="ko-brand"><Image src="/ko-logo.png" alt="KO" width={48} height={48}/><span>KO<span>•</span></span></Link><Link className="ko-login" href="/shop">العودة للمتجر</Link></header><section className="ko-section product-detail"><div className="ko-product-image">{product.image_url?<Image src={product.image_url} alt={product.name} fill sizes="50vw"/>:<Image src="/ko-logo.png" alt="KO" fill sizes="50vw"/>}</div><div><p className="ko-kicker">{product.category||"KO STORE"}</p><h1>{product.name}</h1><p>{product.description}</p><b>{new Intl.NumberFormat("ar-SY").format(product.sale_price)} ل.س</b><p className={product.stock_quantity>0?"stock-ok":"stock-out"}>{product.stock_quantity>0?`متوفر في النادي · ${product.stock_quantity} قطعة`:"نفد المخزون حالياً"}</p><Link className="ko-btn primary" href="/contact">اطلبه عبر واتساب</Link></div></section></main>}
